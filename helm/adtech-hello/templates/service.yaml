apiVersion: v1
kind: Service
metadata:
  name: {{ include "adtech-hello.fullname" . }}
  labels:
    {{- include "adtech-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adtech-hello.selectorLabels" . | nindent 4 }}
